<div class="row">
  <div class="span6 offset3">
    <div class="well">
      <h1 class="center"><%= t('.title') %></h1>

      <%= render 'form', event: @event, submit_text: t('.form_submit') %>
    </div>
  </div>
</div>
