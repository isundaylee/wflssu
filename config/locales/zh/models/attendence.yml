---
zh:
  activerecord:
    models:
      attendence: 签到
    attributes:
      attendence:
        member: 成员
        event: 活动
        state: 状态
  models:
    attendence:
      state_pending: 待审核
      state_accepted: 已批准
      state_rejected: 已驳回
